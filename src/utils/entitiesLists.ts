const rolesList = ['admin', 'user'];
const foodEntitiesList = ['products', 'dishes', 'meals'];

export enum roles {
    ADMIN = 'admin',
    USER = 'user',
}
export enum messageTypes {
    ERROR = 'error',
    SUCCESS = 'success',
}

// export default {
//     roles,
//     foodEntitiesList
// };